/**
 * Cosmos Design System — Animated Icons
 *
 * Powered by Motion (motion/react) + Lucide SVG paths
 * Inspirado em lucide-animated.com
 *
 * Dependências necessárias:
 *   yarn add motion lucide-react
 *
 * Uso:
 *   import { CheckIcon, HeartIcon, MenuIcon } from '@cosmos/icons'
 *
 *   <CheckIcon size={24} color="#6c3fff" strokeWidth={2} />
 *   <MenuIcon />          // clique para toggle hamburger → X
 *   <LoaderIcon spinning /> // gira continuamente
 */

export * from './generated'